'use client';

import DeptArchivePanel from '@/components/asset/production/DeptArchivePanel';

/** 부서 명세서 정산 — 수령완료 이관 후 · 마스터 보관함 이동 전 */
export default function DeptSettlementPanel() {
  return <DeptArchivePanel variant="dept" />;
}
