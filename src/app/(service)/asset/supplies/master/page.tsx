'use client';

import L4PanelRenderer from '@/components/admin/L4PanelRenderer';

export default function MasterIndexPage() {
  // 🚀 usePathname 훅 연동 구조이므로 별도의 props를 주입하지 않습니다.
  return <L4PanelRenderer />;
}